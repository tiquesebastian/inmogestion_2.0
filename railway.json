{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "commands": [
      "npm install",
      "cd .vscode",
      "npm install"
    ]
  },
  "deploy": {
    "command": "cd .vscode && npm start"
  }
}
